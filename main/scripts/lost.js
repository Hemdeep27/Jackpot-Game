const retry = document.getElementById("retry");
const homeDiv = document.getElementById("app");
const loosingDiv = document.getElementById("lose-container");

retry.addEventListener("click", ()=> {
    window.location.reload();
})